<div class="container">
  <div class="table-container">
    <h3><%= "Book tickets for:" %></h3>

    <%= render 'form', booking: @booking %>
    <hr class="mb-3">
    <%= link_to 'Back to Show Details', show_showing_path(@showing.show, @showing) %>
  </div>
</div>
